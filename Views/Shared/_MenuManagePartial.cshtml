@using Microsoft.AspNetCore.Identity
@inject UserManager<AppUser> userManager
@{
    string username = "";
    string userid = "";
    bool isAdmin = false;
    if (User.Identity.IsAuthenticated)
    {
        if(this.User.IsInRole("Administrator"))
        {
            isAdmin = true;
        }
        else
        {
            isAdmin = false;
        }
        userid = userManager.GetUserId(User);
        username = userManager.GetUserName(User);
    }

}
<div class="dropdown">
    <button class="btn btn-secondary dropdown-toggle" type="button" data-bs-toggle="dropdown" aria-expanded="false">
        Dropdown button
    </button>
    <ul class="dropdown-menu">
        @{
            if(isAdmin)
            {
                <a class="dropdown-item" asp-action="Index" asp-controller="Genre" asp-area="Admin">
                    Genres Manage
                </a>
                <a class="dropdown-item" asp-action="Index" asp-controller="Tag" asp-area="Admin">
                    Tag Manager
                </a>
                <a class="dropdown-item" asp-action="Index" asp-controller="Manage" asp-area="Identity">
                    Manage User(admin)
                </a>
                <a class="dropdown-item" asp-action="Index" asp-controller="Role" asp-area="Identity">
                    Manage Role(Admin)
                </a>
            }
        }
        
        <a class="dropdown-item" asp-area="Blog" asp-controller="ProjectPage" asp-action="Index">
            Dashboard
        </a>
        <a class="dropdown-item" asp-action="Index" asp-controller="Manage" asp-area="Identity">
            View Profile
        </a>
 
    </ul>
</div>

@*<div class="dropdown">
    <button class="btn btn-outline-danger dropdown-toggle" href="#"
        id="managedropdownmenu" data-toggle="dropdown">
        Manage
    </button>
    <div class="dropdown-menu" aria-labelledby="managedropdownmenu">
        <a class="dropdown-item" asp-action="Index" asp-controller="DbManage" asp-area="Database">
            Database
        </a>

    </div>
</div>*@